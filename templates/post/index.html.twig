{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
    <ul>
        {% for post in posts %}
            <li>{{ post.title }}</li>
        {% else %}
            <li>Pas de Posts</li>
        {% endfor %}
    </ul>

{% endblock %}